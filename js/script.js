// users data input
const distance = parseInt(prompt('Quanti km vuoi percorrere?'));

const age = parseInt(prompt('Quanti anni hai?'));

const priceKm = 0.21;

let ticketPrice = prompt(priceKm * distance);
